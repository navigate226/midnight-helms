apiVersion: v1
kind: Service
metadata:
  name: {{ include "proof-server.fullname" . }}
  labels:
    app: {{ include "proof-server.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
  selector:
    app: {{ include "proof-server.name" . }}
